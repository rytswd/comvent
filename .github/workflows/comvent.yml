name: Comvent Parse
on: issue_comment

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - name: lalala
        run: |
          echo This is a test
          set
      - uses: .@master
        with:
          keyword: ghi
